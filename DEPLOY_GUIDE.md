# 🚀 云函数部署指南 - 解决超时问题

## 问题原因
错误码 `-504003` 表示云函数执行超时（默认3秒），但 AI 生成需要 5-15 秒。

虽然 `config.json` 已配置 `timeout: 60`，但**配置未生效**，需要重新部署。

---

## ✅ 操作步骤（必须按顺序执行）

### 步骤 1：打开微信开发者工具
确保已打开「周报pro」项目

### 步骤 2：定位到 apiProxy 云函数
在左侧文件树中找到：
```
cloudfunctions/
└── apiProxy/        ← 右键点击这个文件夹
```

### 步骤 3：上传并部署云函数
右键 `apiProxy` 文件夹 → 选择：

```
📤 上传并部署：云端安装依赖（推荐）
   或
📤 上传并部署：所有文件
```

**等待提示**: "上传成功" 或 "部署完成"

### 步骤 4：验证部署成功
1. 点击顶部菜单栏「云开发」按钮
2. 进入「云开发控制台」
3. 左侧选择「云函数」
4. 找到 `apiProxy` 函数
5. 点击进入详情页
6. 查看「函数配置」确认：
   - ✅ 超时时间：**60秒**
   - ✅ 内存大小：**256 MB**

### 步骤 5：测试生成功能
回到小程序模拟器：
1. 填写测试数据
2. 点击「生成专业周报」
3. 观察是否正常生成

---

## ⚠️ 如果上述步骤后仍然报错

请检查以下项：

### 1️⃣ 云开发环境是否正确
在 [app.js](file:///d:/周报pro/app.js) 和 [index.js](file:///d:/周报pro/pages/index/index.js) 中确认：
```javascript
wx.cloud.init({
  env: 'cloud1-d6gx2o3nae6823c0d'  // ← 环境ID必须一致
})
```

### 2️⃣ 云开发是否已开通
1. 微信开发者工具 → 「云开发」按钮
2. 如果提示开通，按指引开通（需要实名认证）
3. 选择或创建环境：`cloud1-d6gx2o3nae6823c0d`

### 3️⃣ apiProxy/config.js 中 API Key 是否有效
打开 [apiProxy/config.js](file:///d:/周报pro/cloudfunctions/apiProxy/config.js) 确认：
```javascript
apiKey: 'sk-95d6993d5f7d49bd8c5c6d94288e6300'  // ← 确认有效
```

验证方法：访问 https://platform.deepseek.com 检查余额和 Key 状态

---

## 🎯 预期结果

部署成功后，控制台应显示：
```
✅ 记录第 1 次生成，剩余 4 次
📤 调用 apiProxy 云函数 (安全代理模式)
✅ API Key 已安全存储在云端，前端无法访问
✅ 云函数调用成功！耗时: X.XX秒    ← 应该 > 3秒
🎉 周报生成成功！
```

---

## 💡 备选方案（如果无法部署云函数）

如果你暂时无法部署云函数（如未开通云开发、网络问题等），我可以帮你：

**临时回退到前端直连模式**（带安全警告）

但这会重新暴露 API Key，仅建议用于：
- 本地测试
- 临时紧急使用
- 生产环境前务必切换回云函数模式

---

## 📞 需要帮助？

如果遇到问题，请告诉我：
1. 云开发控制台的截图
2. 具体的错误信息
3. 你执行到了哪一步

我会继续帮你解决！💪
